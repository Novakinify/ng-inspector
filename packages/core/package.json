{
  "name": "@ng-inspector/core",
  "version": "0.0.0",
  "description": "Core audit library for ng-inspector",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test"
  }
}
